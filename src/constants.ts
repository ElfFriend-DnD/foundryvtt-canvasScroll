export const MODULE_ID = 'canvas-scroll';

export enum MySettings {
  enable = 'enable-canvas-scroll',
  scrollMultiplier = 'scroll-multiplier',
  smoothScrollingDuration = 'smooth-scrolling-duration',
}
